(function($) {
	/*var dropdownDiv = $('.js-dropdown');
	var buttonDiv = $('.js-button');
	var listDiv = $('.js-list');
	buttonDiv.click(function() {
		var parent = $(this).closest(dropdownDiv);
		if(parent.hasClass('active')) {
			parent.removeClass('active');
			parent.find(listDiv).slideUp();
		} else {
			parent.addClass('active');
			parent.find(listDiv).slideDown();
		}
	});
	// Driver tooltip
	var markerDiv = $('.js-marker');
	var pinDiv = $('.js-pin');
	var infoDiv = $('.js-info');
	pinDiv.click(function() {
		var parent = $(this).closest(markerDiv);
		parent.find(infoDiv).toggle();
	});*/

	/*$(document).click(function(e){
	    e.stopPropagation();
	    //console.log(e);
 		var btnDatePicker = $(".js-button");
	 	if(!btnDatePicker.is(e.target)){
	    	$(".js-list").fadeOut(500);
	  	}
	});*/

})(jQuery);